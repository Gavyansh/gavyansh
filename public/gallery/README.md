# Gallery media

Add **images** (JPEG, PNG, WebP) and **videos** (MP4) here. They are served at `/gallery/your-file.ext`.

Update the list in `src/constants.ts` → `GALLERY_MEDIA` when you add or remove files.
